# Presentation archive

This folder preserves earlier figure assets and revision notes that are not part of the current Wiki display. The paired five-construct class heatmap and non-TRM structural map were removed from the current page at the author's request.

The current four-figure set is listed in the [Wiki gallery](../../wiki/figures/captions.md). `add_wiki_figures_previous.py` is a historical source snapshot with pre-reorganization paths, retained for provenance rather than as the current execution entry point. Use the scripts under `wiki/scripts/` to regenerate the current figures.
