# Scaffold input caches

These inputs support the retained PUF scaffold analyses. They contain the original construct-level feature records, matrix caches and residue mappings required by the architecture workflow.

See `provenance.json` for migration checks and original hashes. Historical source paths identify the original analysis layout; the files now reside below `research/`. Raw numerical values and construct identifiers are unchanged.
