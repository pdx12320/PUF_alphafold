# Fixed-test figure review

The source validator does not resolve the imported millimetre conversion constant; final Matplotlib canvas sizes were verified as 183 × 85 mm, 120 × 85 mm and 183 × 84 mm. PNG at 600 dpi is the intended Wiki raster format; SVG and PDF provide editable vector copies. These account for the width/raster advisory findings.

Final PDF text and collision checks pass for all three figures. Rendered multi-panel geometry passes the 1.5 pt alignment gate. Figure 1 contains every fixed-four prediction; Figure 2 deliberately includes only the twenty training constructs; Figure 3 contains every fixed-five endpoint prediction. The S12 distribution remains descriptive. No rows were removed based on prediction errors.
